<% if scope.lookupvar('bacula::params::ssl') -%>
    TLS Enable              = yes
    TLS Require             = yes
    TLS CA Certificate File = <%= @conf_dir %>/ssl/ca.pem
    TLS Verify Peer         = no
<% end -%>
